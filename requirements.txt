# Hiscox Digital Atlas — Streamlit deployment dependencies
# Pinned for a stable, reproducible deployment.

streamlit==1.52.1
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
sentence-transformers==3.0.1
openpyxl==3.1.5          # required for .xlsx uploads (fixes the ImportError)
